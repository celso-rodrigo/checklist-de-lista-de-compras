function EditMenu() {
  return (
    <div>
      <button
        type="button"
        onClick={() => console.log("WIP")}
      >
        Editar
      </button>
      <button
        type="button"
        onClick={() => console.log("WIP")}
      >
        Excluir
      </button>
    </div>
  )
}

export default EditMenu